name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - run: docker compose run --build --rm test

  build:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            suffix: amd64
          - target: aarch64-unknown-linux-musl
            suffix: arm64
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: mlugg/setup-zig@v2
      - uses: taiki-e/install-action@cargo-zigbuild
      - name: Build and package
        run: |
          cargo zigbuild --release --target ${{ matrix.target }}
          chmod +x "target/${{ matrix.target }}/release/xstrip"
          tar -czf "xstrip-linux-${{ matrix.suffix }}.tar.gz" \
              -C "target/${{ matrix.target }}/release" xstrip
      - uses: actions/upload-artifact@v4
        with:
          name: xstrip-linux-${{ matrix.suffix }}
          path: xstrip-linux-${{ matrix.suffix }}.tar.gz
          retention-days: 30
