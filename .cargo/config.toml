# Local builds: cargo build --release --target <triple>
# Docker builds: xx-cargo selects target from TARGETPLATFORM automatically

[target.x86_64-unknown-linux-musl]

[target.aarch64-unknown-linux-musl]
